import MessagesPage from '@/components/messages/Meassage';
import React from 'react';

const page = () => {
    return (
        <div>
            <MessagesPage />   
        </div>
    );
};

export default page;